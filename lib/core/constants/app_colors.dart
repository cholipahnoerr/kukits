import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF7BAE7F);        // Natural Sage Green
  static const primaryDark = Color(0xFF4A5D4E);    // Deep Forest

  // Accent
  static const accent = Color(0xFFE6A05D);         // Warm Wheat Amber

  // Background & Surface
  static const background = Color(0xFFFDFBF7);     // Steamed Wheat
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF2D3436);    // Natural Charcoal
  static const textSecondary = Color(0xFF7F8C8D);

  // Border & Divider
  static const divider = Color(0xFFE8E4D9);        // Almond Soft
  static const cardBorder = Color(0xFFE8E4D9);

  // Semantic
  static const error = Color(0xFFE57373);
  static const success = Color(0xFF7BAE7F);
  static const warning = Color(0xFFE6A05D);
}